hello git!
git is very good!
add other line
Git has a mutable index called stage
Git tracks changes of files
git checkout return to last commit or add status
(git checkout -b branchName) or (git branch branchName, git checkout branchName) create a branch and point to new branch;
Creating a new branch is quick & simple.
